// R donner choix au joueur (pierre fueille ciseaux)
// A : rien
// R : choix du joueur

function ChoixJoueur() {
    let choixJ = prompt("Choisis Papier Cailloux Ciseaux");
    return choixJ;
}

// R : Ordi choisis choisir nombre aléatoire entre 1 et 3
// 1 = Papier 2= Ciseaux 3= Pierre
// A : rien
// R : Le choix de l'ordinateursous forme de string
function ChoixOrdinateur() {
    return Math.ceil((Math.random)*3)
}
// R : Convertir les chiffres en mots
// A : le nombre de la function ChoixOrdinateur
// R : le mot
function Convertion(nbr) {
    if (nbr===1){
        nrb=
    }
}

// R : Comparer le choix du joueur et de l'ordi
// A : ChoixJoueur et ChoixOrdinateur
// R : Le gagnant

function Comparaison(ChoixJoueur, ChoixOrdinateur) {
    
    let result;

    if (ChoixJoueur === ChoixOrdinateur) {
        let gagnant ="égalité"
    } else if (ChoixJoueur===Ciseaux)
}



// R :
// A :
// R :

// R :
// A :
// R :

// R :
// A :
// R :

// R :
// A :
// R :